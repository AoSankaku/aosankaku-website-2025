diff --git a/node_modules/@astro-community/astro-embed-youtube/.bun-tag-78de75ad5a05ba8 b/.bun-tag-78de75ad5a05ba8
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@astro-community/astro-embed-youtube/.bun-tag-953d63c8762b33b6 b/.bun-tag-953d63c8762b33b6
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@astro-community/astro-embed-youtube/.bun-tag-c296f4c3cab15ae8 b/.bun-tag-c296f4c3cab15ae8
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/YouTube.astro b/YouTube.astro
index 6f1375bc8ea1afadfdd0c01710cdc0a005602c45..87cc43d920f23cfdd2cc526964c488b38dc07188 100644
--- a/YouTube.astro
+++ b/YouTube.astro
@@ -1,69 +1,97 @@
 ---
 import 'lite-youtube-embed/src/lite-yt-embed.css';
+import { getImage } from "astro:assets";
 import urlMatcher from './matcher';
 
+const BLACK_SQUARE = "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=";
+
 export interface Props extends astroHTML.JSX.HTMLAttributes {
-	id: string;
-	poster?: string;
-	posterQuality?: 'max' | 'high' | 'default' | 'low';
-	params?: string;
-	playlabel?: string;
+  id: string;
+  poster?: string;
+  posterQuality?: 'max' | 'high' | 'default' | 'low';
+  params?: string;
+  playlabel?: string;
+  fetchpriority?: 'high' | 'low' | 'auto';
+	compressionQuality?: number;
 }
 
 const {
-	id,
-	poster,
-	posterQuality = 'default',
-	title,
-	style,
-	...attrs
+  id,
+  poster,
+  posterQuality = 'default',
+  title,
+  style,
+  fetchpriority = 'auto',
+	compressionQuality = 75,
+  ...attrs
 } = Astro.props as Props;
 
 const idRegExp = /^[A-Za-z0-9-_]+$/;
 
 function extractID(idOrUrl: string) {
-	if (idRegExp.test(idOrUrl)) return idOrUrl;
-	return urlMatcher(idOrUrl);
+  if (idRegExp.test(idOrUrl)) return idOrUrl;
+  return urlMatcher(idOrUrl);
 }
 
 const videoid = extractID(id);
 const posterFile =
-	{
-		max: 'maxresdefault',
-		high: 'sddefault',
-		default: 'hqdefault',
-		low: 'default',
-	}[posterQuality] || 'hqdefault';
-const posterURL =
-	poster || `https://i.ytimg.com/vi/${videoid}/${posterFile}.jpg`;
+  {
+    max: 'maxresdefault',
+    high: 'sddefault',
+    default: 'hqdefault',
+    low: 'default',
+  }[posterQuality] || 'hqdefault';
+
+const posterURLRaw =
+  poster || `https://i.ytimg.com/vi/${videoid}/${posterFile}.jpg`;
+
+const optimizedPoster = await getImage({
+  src: posterURLRaw,
+  format: 'webp',
+  inferSize: true,
+  quality: compressionQuality,
+  width: posterQuality === 'max' ? 1280 : 640 
+}).catch(() => ({
+  src: BLACK_SQUARE,
+  width: posterQuality === 'max' ? 1280 : 640 
+}));
+
 const href = `https://youtube.com/watch?v=${videoid}`;
 
 const styles = [];
 if (style) styles.push(style);
-if (posterURL) styles.push(`background-image: url('${posterURL}')`);
+if (optimizedPoster.src) styles.push(`background-image: url('${optimizedPoster.src}')`);
 ---
 
+<link
+  rel="preload"
+  as="image"
+  href={optimizedPoster.src}
+  fetchpriority={fetchpriority}
+/>
+
 <lite-youtube
-	{videoid}
-	{title}
-	data-title={title}
-	{...attrs}
-	style={styles.join(';')}
+  {videoid}
+  {title}
+  data-title={title}
+  fetchpriority={fetchpriority}
+  {...attrs}
+  style={styles.join(';')}
 >
-	<a {href} class="lyt-playbtn">
-		<span class="lyt-visually-hidden">{attrs.playlabel || 'Play'}</span>
-	</a>
+  <a {href} class="lyt-playbtn">
+    <span class="lyt-visually-hidden">{attrs.playlabel || 'Play'}</span>
+  </a>
 </lite-youtube>
 
 <script src="lite-youtube-embed"></script>
 
 <style is:global>
-	lite-youtube > iframe {
-		all: unset !important;
-		width: 100% !important;
-		height: 100% !important;
-		position: absolute !important;
-		inset: 0 !important;
-		border: 0 !important;
-	}
+  lite-youtube > iframe {
+    all: unset !important;
+    width: 100% !important;
+    height: 100% !important;
+    position: absolute !important;
+    inset: 0 !important;
+    border: 0 !important;
+  }
 </style>
